# === Framework cốt lõi ===
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# === Cơ sở dữ liệu & ORM ===
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.0
pgvector==0.3.5

# === Bảo mật & Xác thực ===
passlib[bcrypt]==1.7.4
pyjwt==2.9.0
python-dotenv==1.0.1

# === Chống tấn công & Rate Limiting ===
slowapi==0.1.9

# === Hàng đợi tác vụ bất đồng bộ (RabbitMQ) ===
pika==1.3.2

# === Tích hợp AI (Google Gemini) ===
google-generativeai==0.8.0

# === Bộ nhớ đệm (Redis) ===
redis[hiredis]==5.1.0

# === Lưu trữ ảnh (Cloudinary) ===
cloudinary==1.41.0

# === Thanh toán (VNPAY) ===
httpx==0.27.0
